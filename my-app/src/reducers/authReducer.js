const initialState = {
  token: null,
};

export default function authReducer(state= initialState,action) {
    cosnt (type,payload) = action

    switch(type){
        default:
            return state
    }

}